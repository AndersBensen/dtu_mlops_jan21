name: "Run tests"

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r day2/m5_m6_m7_m8/dtu_mlops_jan21/requirements.txt
        pip install -r day2/m5_m6_m7_m8/dtu_mlops_jan21/requirements_tests.txt
    - name: Test with pytest
      run: |
        pip install pytest
        pytest -v

