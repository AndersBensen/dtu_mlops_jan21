from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project which makes a Corrupt Mnist model with CoookieCutter structure',
    author='AndersBensen',
    license='MIT',
)
